// Padrões para variáveis

const varName = "Var"; // Use camelCase
const URL = "url"; // Use upper-case para constantes globais.

// Funções 

function nomeFuncao() {
    return; // Utilizar camelCase para nomes de variaveis 
}

const calc = (a, b) => {
    return a + b; // Utilize arrow-functions para funções simples.
}

// Strings

// Utilize template literals

const message = `Hello ${userName}`;

// Outros 

// -- Utilizar === ao invés de ==
// -- Utilizar ;
// -- Utilizar arquivos separados por função.